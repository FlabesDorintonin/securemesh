# SUPERSEDED
#
# This historical project-wide manifest was generated against an earlier repository layout
# and referenced `.github/workflows/android-debug-apk.yml`, which is not part of the current tree.
# It must not be used as current integrity evidence.
#
# Current v1.0.4 release identities are versioned in:
#   artifacts/v1.0.4/RELEASE_MANIFEST_v1.0.4.sha256
#
# Git commit identity is the authoritative integrity boundary for repository source.
